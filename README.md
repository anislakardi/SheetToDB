# SheetToDB

A minimal data import pipeline that allows uploading Excel sheets and inserting structured table rows into an Oracle database via a Node.js backend.

## 🚀 Overview
ExcelBridge handles:
- Excel file upload from a React interface  
- Table preview of sheet content  
- Data parsing & basic validation on the backend  
- Row insertion into Oracle using an API endpoint 